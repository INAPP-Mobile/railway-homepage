# Node / build artifacts
node_modules/
.next/
.cache/
.turbo/
dist/
build/

# Local environment files (NEVER commit)
.env
.env.local
.env.*.local
.env.production

# Personal dashboard configs (the volume on /app/config)
**/config/*.yaml
!config/settings.example.yaml

# Editor / OS junk
.DS_Store
.idea/
.vscode/
*.swp
*.swo

# Test artifacts (E2E pipeline writes here and gitignores them)
assets/screenshot-*.png
test-results/

# Railway plumbing (auto-generated when the CLI is invoked)
.railway/
*.log
